{
  "name": "client",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "npx node-sass ./src/style/index.scss --output ./public/"
  },
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "node-sass": "^4.13.1"
  }
}
